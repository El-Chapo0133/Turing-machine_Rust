[package]
name = "turing-machine_rust"
version = "0.1.0"
authors = ["lorleveque"]
edition = "2018"

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html

[dependencies]
serde_json = "1.0"
serde = { version = "1.0.123", features = ["derive"] }